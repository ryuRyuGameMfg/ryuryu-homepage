# 2026-04-26 - Mode: weekly_report (iter11)

## 実行結果

- 更新ファイル:
  - `data/csv/ja/news.csv`: id=19 追加（2026-04-25 公開 note 記事）
  - `data/csv/en/news.csv`: id=19 追加 + 欠落 id=18 補完
  - `memory/seo-queue.json`: seo-011 キャンセル、seo-005 ブロック、seo-012/013/014 新規追加
  - `memory/state.json`: iter 10→11
- デプロイ: commit → push → Netlify 自動デプロイ

## 観察・メモ

### 重要構造発見（SEO 戦略の転換点）

| 項目 | 内容 |
|------|------|
| ServicesSection.tsx | `'use client'` + useEffect 内ハードコード静的データ使用。services.csv は API を通じてルートは存在するが ServicesSection では全く読まれない。 |
| NewsSection.tsx | `'use client'` + `/api/news` を非同期 fetch。クライアントレンダリング。 |
| FAQSection.tsx | `'use client'` + `/api/faq` を非同期 fetch。クライアントレンダリング。 |
| layout.tsx の SSR | `<title>`, `<meta description>`, Organization/FAQPage JSON-LD のみ SSR。ただし `'use client'` 宣言あり（Next.js App Router では SSR はされるが metadata API は使えない） |

### CSV 変更の実 SEO 効果

| CSV | 実効性 |
|-----|--------|
| news.csv | クライアント fetch → Google JS レンダリング後にインデックス（遅延あり） |
| services.csv | ServicesSection がハードコードデータ使用 → **SEO 効果ゼロ** |
| faq.csv | クライアント fetch → layout.tsx の FAQ JSON-LD は別の hardcode 5件のみ SSR |
| sections.csv | 各セクション Component が sectionData?.description を描画 → クライアントレンダリング後有効 |
| testimonials.csv | クライアント fetch → 遅延あり |

### セッション 0 の根本原因分析

1. **新ドメイン** (`ryuryugame.netlify.app` サブドメイン) — ドメイン権威ゼロ
2. **被リンクゼロ** — note/Zenn 記事が homepage URL に言及していない
3. **コンテンツほぼクライアントレンダリング** — Google の JS レンダリングキューは遅延あり
4. **GSC サイトマップ提出状況不明**

### 推奨アクション（社長向け）

最優先: note/Zenn 記事末尾またはプロフィールに `https://ryuryugame.netlify.app` の被リンクを追加すること。これがセッション 0→1 に最も直接的に効く。

## 追加アクション（今後のキュー）

| ID | タスク | 優先度 |
|----|-------|--------|
| seo-013 | GSC インデックスカバレッジ確認 + note/Zenn 記事への被リンク追加 | high |
| seo-014 | news.csv 月次更新継続 | medium |
| seo-005 | testimonials.csv 更新（実データ提供待ち） | blocked |
| seo-011 | services.csv detailedDescription リッチ化 | cancelled |

## 課題

- services.csv の変更が無意味なことが判明。ServicesSection のハードコードデータを更新するにはコンポーネント変更が必要だが SOUL 制約で禁止。
- セッション 0 は content-level では解決できない。外部施策（被リンク、GSC 登録）が必要。
- seo-013 は AI が直接実行できないため社長対応が必要。
