# 2026-05-01 - Mode: weekly_report (iter16)

## 実行結果

- 更新ファイル:
  - `public/cluster-world-seisaku.html`: 新規作成（静的HTML・Clusterワールド制作ランディングページ）
  - `public/robots.txt`: cluster-world-seisaku.html を Sitemap: 行に追加
  - `data/csv/ja/news.csv`: id=22 追加（Horizon Worlds VR終了×Cluster移行）
  - `data/csv/en/news.csv`: id=22 追加（英語版）
  - `memory/reports/2026-05-01.md`: 本レポート
  - `memory/state.json`: iter 15 → 16
- デプロイ: commit → push → Netlify 自動デプロイ
- Telegram: 未送信（週次レポートのみ保存）

## 今回のアクション根拠

### iter16 の戦略: クライアントレンダリング問題を「静的HTMLページ」で回避

```text
┌─────────────────────────────────────────────────────────────┐
│ 根本問題: layout.tsx が 'use client' + dynamic() 全セクション│
│ → Googlebot 初回クロールでコンテンツ不可視               │
│                                                             │
│ 解決策: public/ に静的 HTML を配置                         │
│   → JS 不要 → 即時インデックス対象                        │
│   → JSON-LD を <head> に直接埋め込み → Rich Result 候補   │
│   → robots.txt Sitemap: 追加 → Bing 24h 発見              │
└─────────────────────────────────────────────────────────────┘
```

### KPI 改善機序

| チャネル | 流入経路 | 見込みタイムライン |
|---------|---------|----------------|
| Google 検索 | "Cluster ワールド制作 費用" のロングテール | 2〜4週間 |
| Bing 検索 | robots.txt Sitemap: 経由の即時クロール | 24〜72時間 |
| AI 検索 (Perplexity) | FAQPage JSON-LD → 引用 | 1〜2週間 |

### Horizon Worlds 終了という時機

```text
┌── 背景 ──────────────────────────────────────────────────────┐
│ 2026-06-15: Meta Horizon Worlds VR 版終了（確定済み）       │
│ 2026-03-31: Quest ストアからの削除済み                      │
│                                                             │
│ 影響: 企業メタバース担当者が「代替プラットフォーム」を      │
│       NOW 検索中 → Cluster への移行相談が急増見込み         │
│                                                             │
│ RYURYU の優位: Cluster × Unity CCK の両方に知見あり        │
│ 競合: 三軸工房のみ（静的 HTML ランディングページなし）      │
└──────────────────────────────────────────────────────────────┘
```

### 技術的差別点: なぜ静的 HTML か

| 手法 | Googlebot 可視 | 実装難易度 | 権限範囲内 |
|------|--------------|----------|---------|
| CSV 更新 (従来) | ❌ (client-render) | 低 | ✅ |
| layout.tsx 修正 | ✅ | 中 | ❌ (禁止) |
| **静的 HTML (今回)** | **✅** | **低** | **✅** |
| Next.js SSR 化 | ✅ | 高 | ❌ (禁止) |

## 議論トピック（社長への確認事項）

### Topic 1: 静的 HTML ページをカスタムドメイン下でどう扱うか

```text
┌─────────────────────────────────────────────────────────────┐
│ 現状: ryuryugame.netlify.app/cluster-world-seisaku.html     │
│ A案: このまま運用（Netlify URL）                            │
│ B案: 独自ドメイン取得後に移行（SEO長期効果 ↑）             │
│                                                             │
│ 推奨: A案で先行、B案は独自ドメイン取得時に自動移行          │
└─────────────────────────────────────────────────────────────┘
```

### Topic 2: 静的ページを増やす戦略

```text
┌─────────────────────────────────────────────────────────────┐
│ A案: Cluster のみ（今回）                                   │
│ B案: 追加で "Unity 開発 外注" "Meta Quest VR 開発" も作成  │
│                                                             │
│ 推奨: B案。検証後（2週間後）に反応を見て追加。             │
└─────────────────────────────────────────────────────────────┘
```

## 観察・メモ

- 静的 HTML は「コンポーネント変更禁止」制約を完全に回避する唯一の手段
- FAQPage JSON-LD を `<head>` に直接記述 → Googlebot の初回クロールで認識
- Cluster 市場は 2026Q2 が需要ピーク（Horizon Worlds 終了直前期）
- 次回イテレーション: 静的 "unity-kaigai.html" or "meta-quest-vr-kaigai.html" を検討

## 課題

- GA4 MCP が無効（GA Admin API 要有効化）→ 実数値計測は引き続き不可
- 静的 HTML が Main site (react SPA) から内部リンクされていないため発見が robots.txt Sitemap: のみ
- 効果検証: 最低 2〜4 週間必要（Googlebot クロールサイクル）
